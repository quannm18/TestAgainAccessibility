package com.example.testagainaccessibility.utils

import java.util.concurrent.atomic.AtomicBoolean

object Constant {
    var FORCE_STOP = "Force stop"
    var CACHE = "Storage & cache"
    var isCleanRunning = false
    var isCheckedAll = AtomicBoolean()
    const val OK = "OK"

}